// Single source of truth for the booking/scheduling link used across CTAs.
// Base Calendly link shows all active event types, so it keeps working even if
// individual event types are renamed or added.
export const BOOKING_URL = 'https://calendly.com/catalin-rizea-innoviasystems';
